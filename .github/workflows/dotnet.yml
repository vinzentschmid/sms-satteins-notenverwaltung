name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 7.0.x
    - name: Restore dependencies
      run: dotnet restore BackendRestAPI
    - name: Build
      run: dotnet build BackendRestAPI --no-restore
    - name: Test
      run: dotnet test BackendRestAPI --no-build --verbosity normal

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
    - name: Publish
      run: |
        run: dotnet publish BackendRestAPI/BackendRestAPI -c Release --no-build

    - name: Deploy
      run: |
        # Add your deployment steps here
        # Navigate to the published folder if needed
        # For example, deploy to a server, Azure, AWS, or any other platform

